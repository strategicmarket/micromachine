applications:
-       name: micromachine
        host: micromachine
        buildpack: https://github.com/cloudfoundry/nodejs-buildpack
        memory: 256M
        instances: 1
        domain: mybluemix.net
        path: .
        command: npm run start
        env:
          PRODUCTION: true
